name: CI/CD with SonarQube Quality Gate

on:
  push:
    branches: [ dev ]
  pull_request:
    branches: [ dev ]

jobs:
  build-and-scan:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0  # Required for SonarQube analysis

    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'maven'

    - name: Configure SonarQube Properties
      working-directory: ./kanban-task-management-backend
      run: |
        # Create a temporary file with the profile content
        cat << 'EOF' > sonar-profile.xml
        <profiles>
            <profile>
                <id>sonar</id>
                <properties>
                    <sonar.host.url>${{ secrets.SONAR_HOST_URL }}</sonar.host.url>
                    <sonar.projectKey>kanban-task-manager-infra</sonar.projectKey>
                    <sonar.qualitygate.wait>true</sonar.qualitygate.wait>
                    <sonar.qualitygate.timeout>600</sonar.qualitygate.timeout>
                </properties>
            </profile>
        </profiles>
        EOF

        # Insert the profile only if not already present
        if ! grep -q "<sonar.qualitygate.wait>" pom.xml; then
          # Use awk to safely insert before </project>
          awk -v profile="$(cat sonar-profile.xml)" '1; /<\/project>/ {print profile}' pom.xml > pom.xml.tmp
          mv pom.xml.tmp pom.xml
        fi

    - name: Build and Run SonarQube Analysis
      working-directory: ./kanban-task-management-backend
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      run: |
        # Build and run analysis with Quality Gate check
        mvn -B clean verify sonar:sonar \
          -Psonar \
          -Dsonar.login=$SONAR_TOKEN \
          -Dsonar.verbose=true

        # Alternative if you want to see Quality Gate details
        echo "Quality Gate results available at:"
        grep "dashboard url" target/sonar/report-task.txt | cut -d' ' -f3