# Root project information
sonar.projectKey=kanban-task-manager-backend
sonar.projectName=Kanban Task Manager Backend
sonar.projectVersion=1.0

# Source and language settings
sonar.sources=backend
sonar.language=java
sonar.java.binaries=backend/target

# SonarQube server (adjust if needed)
sonar.host.url=http://localhost:9000

# Optional authentication token (recommended over login)
sonar.login=${SONAR_TOKEN}
